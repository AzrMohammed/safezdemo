
import element_types
from django.core.validators import email_re

# VALIDATED = "VALIDATED"

def(element_type, data, name):


    # element_type_name  = ""
    # element_type_max_length = ""
    # element_name = ""

    switch(element){

    case MOD_DT_NAME[0]:
            return validate_mob(element_type, data, name)

    }


def validate_mob(data):

    if !(string.isnumeric())
        return "is not a valid number"
    elif
